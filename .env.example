# Langfuse 暗号化キー (必須)
# 生成コマンド: openssl rand -hex 32
LANGFUSE_ENCRYPTION_KEY=  # ← ここに64文字のhex文字列を設定

# LiteLLM → Langfuse トレース連携用 API キー
# 手順: Langfuse UIでプロジェクト作成後、Settings > API Keys で発行
LANGFUSE_PUBLIC_KEY=pk-lf-CHANGEME
LANGFUSE_SECRET_KEY=sk-lf-CHANGEME
